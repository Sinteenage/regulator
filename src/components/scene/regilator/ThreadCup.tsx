/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import * as THREE from 'three';
import { useGLTF, useTexture } from '@react-three/drei';
import { GLTF } from 'three-stdlib';

type GLTFResult = GLTF & {
  nodes: {
    Cylinder20: THREE.Mesh;
  };
  materials: {
    ['_9_-_Default.001']: THREE.MeshStandardMaterial;
  };
};

export function ThreadCup(props: JSX.IntrinsicElements['group']) {
    const { nodes } = useGLTF('/assets/models/threadCup.gltf') as GLTFResult;

    const [ blueTexture ] = useTexture([
        '/assets/textures/blue.jpg',
    ]);

    return (
        <group {...props} dispose={null}>
            <mesh
                castShadow
                receiveShadow
                rotation={[-Math.PI / 2, -Math.PI / 8, 0]}
                scale={0.025}
            >
                <primitive object={nodes.Cylinder20.geometry} />
                <meshStandardMaterial map={blueTexture} roughness={0.25} metalness={0.4}/>
            </mesh>
        </group>
    );
}

useGLTF.preload('/assets/models/threadCup.gltf');